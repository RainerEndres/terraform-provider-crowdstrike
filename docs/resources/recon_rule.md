---
page_title: "crowdstrike_recon_rule Resource - crowdstrike"
subcategory: "Falcon Intelligence Recon"
description: |-
  This resource allows you to manage Falcon Intelligence Recon monitoring rules in the CrowdStrike Falcon Platform.
  Recon rules define the monitoring criteria used to discover threats across the dark web, criminal forums, and other online sources.
  API Scopes
  The following API scopes are required:
  Monitoring rules (Falcon Intelligence Recon) | Read & Write
---

# crowdstrike_recon_rule (Resource)

This resource allows you to manage Falcon Intelligence Recon monitoring rules in the CrowdStrike Falcon Platform.

Recon rules define the monitoring criteria used to discover threats across the dark web, criminal forums, and other online sources.

## API Scopes

The following API scopes are required:

- Monitoring rules (Falcon Intelligence Recon) | Read & Write


## Example Usage

```terraform
terraform {
  required_providers {
    crowdstrike = {
      source = "registry.terraform.io/crowdstrike/crowdstrike"
    }
  }
}

provider "crowdstrike" {
  cloud = "us-2"
}

resource "crowdstrike_recon_rule" "example" {
  name        = "example-recon-rule"
  topic       = "SA_DOMAIN"
  filter      = "example.com"
  priority    = "high"
  permissions = "private"

  breach_monitoring_enabled = true
}

output "recon_rule" {
  value = crowdstrike_recon_rule.example
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `filter` (String) The FQL filter used for searching.
- `name` (String) The name of the recon rule.
- `permissions` (String) The access permissions for the recon rule.
- `priority` (String) The priority of the recon rule.
- `topic` (String) The topic of the recon rule. Determines what type of threat intelligence is monitored.

### Optional

- `breach_monitor_only` (Boolean) Whether to monitor only for breach data. Must be used with `breach_monitoring_enabled` set to `true`.
- `breach_monitoring_enabled` (Boolean) Whether to monitor for breach data. Only available for `SA_DOMAIN` and `SA_EMAIL` rule topics.
- `lookback_period` (Number) The duration (in days) for which the rule looks back in the past at first run.
- `match_on_tsq_result_types` (Set of String) The result types to monitor for. Only available for the `SA_TYPOSQUATTING` rule topic.
- `substring_matching_enabled` (Boolean) Whether to monitor for substring matches. Only available for the `SA_TYPOSQUATTING` rule topic.

### Read-Only

- `created_timestamp` (String) The timestamp when the recon rule was created.
- `id` (String) The unique identifier of the recon rule.
- `last_updated` (String) Timestamp of the last Terraform update of the resource.
- `status` (String) The current status of the recon rule.
- `status_message` (String) The detailed status message of the recon rule.
- `updated_timestamp` (String) The timestamp when the recon rule was last updated.

## Import

Import is supported using the following syntax:

```shell
# Recon Rule can be imported by specifying the id.
terraform import crowdstrike_recon_rule.example 7fb858a949034a0cbca175f660f1e769
```
